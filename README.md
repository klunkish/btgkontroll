# BTG Quality Control 2.0.5

Public GitHub Pages build.

This package contains only the static frontend files needed to run the app:
- `index.html`
- `css/`
- `js/`
- `module-map.json`

Supabase is used as backend. Security must be enforced with Supabase Row Level Security (RLS).
